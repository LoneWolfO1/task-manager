# Task Manager

> Work in Progress

A terminal-based task manager built in C using ncurses.

## Dependencies
```bash
sudo apt install gcc make libncurses-dev
```

## How to run
```bash
make taskmanager
./taskmanager
```

## Features (so far)
- Navigate menus with arrow keys
- Create and manage folders
- Tasks stored in linked lists
